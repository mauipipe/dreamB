<div class="content">
    <div ng-repeat="comment in comments">
        <h3>name:{( comment.name + comment.last_name)}</h3>
        <div class="beach-detail">
            Nome spiaggia: {( comment.beach.name)}<br>
            Luogo: {( comment.beach.city)}
        </div>
        <p>{( comment.description)}</p>
    </div>
</div>
